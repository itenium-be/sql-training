# Used by `npm run dev` only. Points at a stack running on your own machine.
# Create .env.development.local (gitignored) to override without touching this file.
VITE_API_URL=http://localhost:8080
VITE_LEADERBOARD_URL=http://localhost:8000
